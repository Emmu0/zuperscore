import React from "react";
// next imports
import { NextPage } from "next";
// hoc
import authWrapper from "lib/hoc/authWrapper";

const Schedules: NextPage = () => {
  return <div></div>;
};

export default Schedules;
// export default authWrapper(Schedules, { authRequired: true, role: "user" });
